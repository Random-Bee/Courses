Parallel and Concurrent Programming Assignment 5

Input is given using command line argument. The program takes the name of file containing value of n (number of threads), k (number of tasks by each thread), lambda1 and lambda2 as input.

Executing the file:

CLH:

(i) Compile the cpp file using following command:
g++ CLH-CO21BTECH11002.cpp -o CLH-CO21BTECH11002.out

(ii) Run the executable file with the name of input file as the command line argument using following command:
./CLH-CO21BTECH11002.out inp-params.txt

MCS:

(i) Compile the cpp file using following command:
g++ MCS-CO21BTECH11002.cpp -o MCS-CO21BTECH11002.out

(ii) Run the executable file with the name of input file as the command line argument using following command:
./MCS-CO21BTECH11002.out inp-params.txt
