Parallel and Concurrent Programming Assignment 3

Input is given using command line argument. The program takes the name of file containing value of capacity (number of threads), numOps (no. of operations each thread will perform), and lambda as input.

Executing the file:

(i) Compile the cpp file using following command:
g++ MRMW_CO21BTECH11002.cpp -o MRMW_CO21BTECH11002.out

(ii) Run the executable file with the name of input file as the command line argument using following command:
./MRMW_CO21BTECH11002.out inp-params.txt

